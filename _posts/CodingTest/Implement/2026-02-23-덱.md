---
title: C++ 백준 10866번 / 덱
date: 2026-02-23 01:58:00 +0900
categories: [ CodingTest ]  
tags:  [ C, C++, Implement ]
---

백준 10866번 문제인 덱 이다.

# 문제   
---------------------------------------

![Desktop View](/assets/img/덱.png){: w="700" h="400" }

---------------------------------------

# 풀이

```c++
#include <bits/stdc++.h>
#include <unordered_map>
#include <algorithm>

using namespace std;

vector<int> deck;

void PushFront(int x) { deck.insert(deck.begin(), x); }
void PushBack(int x) { deck.push_back(x); }
void PopFront() 
{
	if (deck.empty() == true)
	{
		cout << -1 << '\n';
		return;
	}

	cout << deck.front() << '\n';
	deck.erase(deck.begin());
	return;
}

void PopBack()
{
	if (deck.empty() == true)
	{
		cout << -1 << '\n';
		return;
	}

	cout << deck.back() << '\n';
	deck.pop_back();
	return;
}

void Front()
{
	if (deck.empty() == true)
	{
		cout << -1 << '\n';
		return;
	}

	cout << deck.front() << '\n';
	return;
}

void Back()
{
	if (deck.empty() == true)
	{
		cout << -1 << '\n';
		return;
	}

	cout << deck.back() << '\n';
	return;
}

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	cout.tie(NULL);

	int n;
	cin >> n;
	cin.ignore();
	while (--n >= 0)
	{
		string command;
		getline(cin, command);

		if (command.find("push_back") != string::npos)
		{
			int num = stoi(command.substr(command.find(" "), command.size() - 1));
			PushBack(num);
		}
		else if (command.find("push_front") != string::npos)
		{
			int num = stoi(command.substr(command.find(" "), command.size() - 1));
			PushFront(num);
		}
		else if (command.find("pop_front") != string::npos)
			PopFront();
		else if (command.find("pop_back") != string::npos)
			PopBack();
		else if (command.find("size") != string::npos)
			cout << deck.size() << '\n';
		else if (command.find("empty") != string::npos)
			cout << (int)(deck.empty()) << '\n';
		else if (command.find("front") != string::npos)
			Front();
		else if (command.find("back") != string::npos)
			Back();
	}

	return 0;
}
```
---------------------------------------

# 후기

덱의 기능들을 구현하라는 문제였다.

구현할 기능들은 아래와 같다.

- push_front X: 정수 X를 덱의 앞에 넣는다.
- push_back X: 정수 X를 덱의 뒤에 넣는다.
- pop_front: 덱의 가장 앞에 있는 수를 빼고, 그 수를 출력한다. 만약, 덱에 들어있는 정수가 없는 경우에는 -1을 출력한다.
- pop_back: 덱의 가장 뒤에 있는 수를 빼고, 그 수를 출력한다. 만약, 덱에 들어있는 정수가 없는 경우에는 -1을 출력한다.
- size: 덱에 들어있는 정수의 개수를 출력한다.
- empty: 덱이 비어있으면 1을, 아니면 0을 출력한다.
- front: 덱의 가장 앞에 있는 정수를 출력한다. 만약 덱에 들어있는 정수가 없는 경우에는 -1을 출력한다.
- back: 덱의 가장 뒤에 있는 정수를 출력한다. 만약 덱에 들어있는 정수가 없는 경우에는 -1을 출력한다.

 
바~로 원하는대로 구현했는데 다른 분들의 답을보니 어떤분들은 정말 class부터 만들어 덱을 만드신 코드가 있었다.

난 정말 기능만 구현했는데... 내가 이상한건가..(긁적..)




